# Environment Variables
# This project runs entirely client-side and does not require any API keys or secrets.
# No .env file is needed for local development.

# Optional: Override the default port
# PORT=3000

# Optional: Node environment
# NODE_ENV=development
